# docker-web-control
Web UI to manage Docker containers with grouping, auto-start, per-container/group aliases and icons, per-tab refresh, filters, and start/stop/restart actions. Auto-group by project/prefix, restart policy sync, language selector (pt-BR/en). Python HTTPServer + Docker CLI backend;   pure HTML/CSS/JS frontend.
